import { Loader, LoaderContainer } from "./styled-components/Loader";

const PreLoader = () => {
  return (
    <LoaderContainer>
      <Loader />
    </LoaderContainer>
  );
};
export default PreLoader;
